# Historia zmian (auto-generated)

## 29.05.2026

- fix(opis-ai): Gemini odpala ZAWSZE gdy api_key, nawet bez bullet_points z Amazon. Produkty bez ASIN (kierownice MOZA, OLX) szly do fallback ktory robi 1-zdanie 'produkt wyrozniajacy sie solidnym wykonaniem'. Teraz Gemini z samej nazwy + typu + categorii pisze pelny opis.

## 28.05.2026

- fix(version): walidacja last_install_commit jako git hash (7-40 hex). Bug v1.0.97 zapisywal VERSION string '1.0.100' jako commit -> sidebar 'v1.0.100+1.0.100' + baner 'Twoja: 1.0.100' (mismatch). Startup cleanup invalid value.
- feat(allegro): /allegro/duplikaty - batch cleanup duplikatow ofert. Grupuje po ASIN+stan (lub nazwa_norm+stan dla bez-ASIN), pre-zaznacza do zakonczenia wszystkie OPROCZ tej z najwieksza ilosc sprzedazy (chronione). Batch PATCH publication.status=ENDED przez Allegro API. Kafelek w /narzedzia.
- fix(update): wylaczono auto-regenerate CHANGELOG.md przy starcie Flask. Plik byl tracked w gicie ale modyfikowany przy kazdym restartcie -> manualny 'git pull' padal z 'local changes would be overwritten by merge'. Endpoint update mial workaround, manual SSH nie.
- fix(banner): baner gorny zawsze sync z dashboard widgetem. Sync update_available z cache.has_update przy kazdym renderze + reset przy starcie. Bez tego po manualnym git pull baner wisial mimo 'System aktualny'.
- fix(update): wersja w sidebar + banner zgadza sie po update (canonical = last_install_commit zamiast git rev-parse). Banner JS fallback do ZIP tylko na 404 (nie na 5xx git pull). Cache cleared po obu paths.
- ux(W4): port health-check przed os._exit po update -> klient nie widzi 'app nie dziala' przez 5-10s gdy Defender skanuje pythonw.exe. + docs/CLIENT_INSTALL_WINDOWS.md - pelna instrukcja dla klienta koncowego.
- sec(W1+W3+S5): vendor-notify proxy (klient nigdy nie ma vendor_bot_token) + DB backup VACUUM INTO przed update + auto-rollback przy failed swap. Patrz commit.
- sec(critical): K1+K3+K4 security fixy przed dostawa SaaS klientom. Patrz commit message.
- perf(db): 3 brakujace indeksy - produkty.kod_magazynowy (skaner/search), oferty.status, oferty(status, data_aktualizacji). Skaner ~10x szybszy przy 50K produktow.
- feat(narzedzia): /magazyn/naprawa-cen-excel - retro-fix dla produktow z brakujacymi cenami. Wgraj Excel, match po ASIN/EAN/nazwa, UPDATE tylko cen (cena_netto+cena_brutto+cena_allegro). Dry-run domyslnie wlaczony - podglad przed zapisem. Kafelek w /narzedzia.
- fix(import): rozszerzony auto-detect kolumn cen + robust parser. CENAZAKUPU/ZAKUP/KOSZT/BUY/PURCHASE/CENABRUTTO dla cena_netto, ALLEGRO/SPRZEDAZ dla cena_allegro. Parser usuwa /szt, *, (), waluty (PLN/EUR/GBP/kn) regex. Bez tego klient z 'Cena zakupu' w Excelu mial wszedzie 0.
- feat(paletomat): "Wystaw BEZ zdjec" toggle w mass-create-from-paleta (checkbox pod cennikiem). Pomija upload + CDN fallback - oszczednosc 5-15s/oferta. Backend &skip_images=1 param. Klient dodaje zdjecia recznie na Allegro pozniej.
- feat(update): ZIP install (Macek) tez ma background update check + banner. _public_update_check_async() pobiera VERSION z public repo, ustawia update_available. Banner JS rozszerzony 3-poziomowy fallback (update-git -> update-from-public -> /system/update).
- feat(gpsr): wybor domyslnego producenta + osoby odpowiedzialnej w /allegro/config (dropdown z listy Allegro) - zamiast [0] losowego z listy. Puste = fallback do pierwszego.
- ux(narzedzia): wywalono sekcje KALKULATORY (oplacalnosci + marzy) - malo uzywane, endpointy zostaja dostepne przez URL
- fix(paletomat): anti-duplicate request lock (60s TTL per user+ids) chroni przed EventSource auto-reconnect i double-submit - 1 klik = 1 oferta na Allegro
- fix(paletomat): dedup widzi szkice (status='draft') - drugi klik "Wystaw bezposrednio" zwieksza ilosc na istniejacym szkicu zamiast tworzyc duplikat
- test(update): smoke test mechanizmu update z banera (bez zmian funkcjonalnych, tylko VERSION bump)
- fix(banner): JS fallback do /system/update jak /admin/update-git zwroci 404 (stary serwer bez restartu Flask po git pull samonaprawia sie)
- ux(layout): wyrazny cyan glow + badge "WYBRANO" + scale 1.015 + animacja pop -> Maciek nie przegapi ze klik zmienil layout
- fix(paletomat): dedup hierarchiczny ASIN > EAN > nazwa_norm dla produktow BEZ ASIN (kierownice MOZA, OLX towar bez kodow tez sie dedupuje)
- fix(paletomat): deduplikacja per ASIN+stan PRZED petla wystawiania (Wystaw bezposrednio nie dubluje juz ofert z roznych palet)
- fix(layout+banner): label onclick zaznacza karte (closest nie dzialal z position:absolute) + banner fetch zamiast form submit (nie zostawia 404 URL)
- fix(import): deduplikacja przy multi-paleta import (UPDATE jak istnieje w palecie + ASIN, INSERT jak nowy)
- fix(scraper): GET /paletomat/scraper/file -> redirect do parent (zamiast 405 Method Not Allowed po F5/Back)
- feat(magazyn): scal duplikaty (po multi-paleta re-imporcie) - sumuje ilosci grup, kasuje osierocone
- fix(sse): keepalive jako data event zamiast comment (proxy ngrok/cloudflare filtruja : komentarze)
- fix(zdjecia): URL format Amazon P/ + naprawia stary I/ format + obsluguje rozne prefixy ASIN
- fix(naprawa): SQL GLOB nie matchowal - usuniety, LENGTH 10-13 zamiast 10
- fix(ean/asin): NIE kopiuj ASIN do EAN przy import + naprawa istniejacych (przenies B0XXXXXXXX z ean -> asin)
- feat(zdjecia): auto-fill Amazon URL przy starcie aplikacji (background thread 10s opoznienia)
- feat(zdjecia): Amazon URL fallback przy multi-paleta import + kafelek 'Uzupelnij zdjecia' dla istniejacych produktow
- feat: 4 zbiorcze fixy (DB lock retry + mata 46870 + dashboard sync + parser)
- fix(banner): auto-detect git vs ZIP install + odpal wlasciwy update
- hotfix(banner): wyczysc update_available po udanym git pull (banner wisial)
- hotfix(banner): /admin/update-git endpoint alias - klik banera dawal 404
- feat(license): TRYB SELF-HOSTED - wylacz walidacje HMAC (klient hostuje sam)
- fix(license): wyrazny komunikat gdy brak .license_secret a baza ma licencje
- fix(update): PRESERVE_PATHS + vendor_config.json + branding (safety net)
- feat(update): pobieranie z PUBLIC repo Trupson2/akces-hub (bez tokena)
- feat(diagnoza): endpoint /narzedzia/diagnoza-cen - znajdz suspicious ceny
- fix(ustawienia): info dla git-install klientow ze token NIE jest potrzebny
- fix(brand): logo klienta NIE nadpisywane przy git pull (custom branding)
- fix(update): klient widzi banner 'Dostepna aktualizacja' automatycznie
- hotfix(encoding): usun BOM (U+FEFF) z 5 plikow modules/ - crash Pythona
- style(format): separator tysiecy dla wszystkich kwot zl w dashboardach
- fix(magazyn): wartosc magazynu = aktualny stan (× ilosc + sprzedane odjete)
- fix(auth): 404 GET /static/True - brand_logo jako boolean zamiast filename
- fix(offline): wywal hardcoded 'ngrok' z offline page (mylil klientow CF)
- hotfix(allegro): napraw DUPLIKATY ofert - retry POST nie-idempotentne
- hotfix(allegro): get_config_cached not defined przy wystawianiu oferty
- feat(layouty): 5ty layout 'Bloki' + piktogramy + fix bialego przycisku
- feat(allegro): konfigurowalny layout opisu (4 uklady sekcji)
- feat(ai): custom prompt do Gemini per klient (nie jak u dostawcy)
- feat(import-multi): 3-krokowy wizard z manualnym mappingiem kolumn
- feat(sidebar): link 'Statystyki' (SALES_HUB) w sekcji Dashboard
- feat(sidebar): link 'Import multi-paleta' w bocznym menu Magazyn
- fix(backup): mini-batche zeby nie blokowac writers (root cause locks 03:43)
- feat(import): multi-paleta import z Excela CRM Online (kolumna NR PALETY)
- fix(allegro): zwieksz retry _persist_tokens 3->8 (incydent 2026-05-28 03:44)

## 27.05.2026

- feat(palety): per-row dropdown cennika propaguje sie do mass-create stream
- feat(currency): backfill stare zamowienia z Allegro (HUF/CZK/EUR -> PLN po NBP)
- feat(currency): sync zamowien konwertuje HUF/CZK/EUR -> PLN po aktualnym kursie NBP
- feat(currency): create_offer akceptuje parametr currency + config default_currency
- feat(currency): NBP API live rates (modules/fx_rates.py)
- fix(palety): pre-render shipping_panel + dla_siebie_panel (Python 3.11 NESTED f-string)
- hotfix(palety): fix komentarz JS shipping_map ktory powodowal 500 w mass-edit
- rollback to v1.0.20 codebase (stabilny - sprzed eksperymentow z dropdown/currency)
- Revert "fix(sse): auto-reconnect SSE po zerwaniu (max 3 proby)"
- fix(sse): auto-reconnect SSE po zerwaniu (max 3 proby)
- fix(palety): KOMENTARZ JS w f-string Python interpretowal jako format spec
- perf(sse): keepalive co 1s (zamiast 3s) - mniej zerwanych polaczen przy uploadzie
- fix(palety): pre-render ship_cell_html (NESTED f-string Python 3.11 fail)
- feat(currency+returns): NBP live + backfill walut + sync zwrotow 12mc
- feat(currency): NBP API live rates + zachowanie oryginalnej waluty + backfill
- fix(currency): KONWERSJA HUF/CZK/EUR -> PLN przy sync zamowien
- feat(currency): obsluga walut EUR/CZK/HUF (Czechy/Slowacja/Wegry)
- fix(setup): auto-skip wizard gdy klient ma juz license + brand_name
- fix(palety): wywal NESTED f-string ktore lamie Python 3.11 (klient embedded)
- feat(license): multi-HWID per plan (MAX=3, ENTERPRISE=5)
- feat(license): self-service HWID reset (limit 3) + Telegram alert do dostawcy
- perf(pakowanie): cache 30s dla /api/wysylki/pending (5-10x szybciej)
- feat(update): Telegram notify gdy nowa wersja dostepna (co 6h)
- feat(wysylki): czytelne REF na etykiecie + custom override
- fix(wysylki): dodaj reference (lokalizacja/SKU) do payloadu Wyslam z Allegro
- fix(licencje): pozwol usunac licencje ze spacjami/polskimi znakami w filename
- feat(palety): per-row dropdown cennika + bulk apply w mass-edit
- feat(palety): dropdown cennika wysylki w /palety/X/mass-edit (Wystaw bezposrednio)
- feat(paletomat): UI dropdown wyboru cennika przy masowym wystawianiu
- feat(allegro): create_offer akceptuje shipping_id_override (per-produkt cennik)
- fix(allegro): neutralne nazwy kurierow + pole InPost paczkomat
- fix(privacy): puste placeholdery dla Allegro UUIDs + hint co wpisac
- fix(update): restart_python_process odpala NOWY proces (DETACHED) — koniec localhost-down
- cleanup: wywal test text z kafelka Update, zostaw clean UI dla klienta
- test(update): v1.0.10 - druga iteracja testu update (rakieta zamiast pioruna)
- fix(update): dodaj .license/.lic/installer/python do PRESERVE_PATHS
- fix(support): wersja z pliku VERSION (nie circular import z app.py)
- bump: v1.0.9
- fix(support): emoji zamiast <span> w Telegram message (HTTP 400 fix)
- bump: v1.0.8 (vendor config embedded test)
- feat(support): vendor credentials embedded + spinner + material icons
- fix(privacy): wywal hardcoded Allegro UUIDs (Adriana) z config defaults
- fix(update): download zip z PRIVATE repo uzywa Bearer token + asset API URL
- test(update): v1.0.6 z widocznym tekstem zeby sprawdzic ze auto-update dziala
- fix(installer): embedded Python widzi modules/ + bump v1.0.4
- bump: v1.0.3
- fix(pwa): NIE rejestruj service workera na localhost (klient Windows)
- fix(ui): dodaj link do /magazyn/kody-magazynowe w /ustawienia
- fix(ui): single-project tenant pomija launcher (redirect /dashboard)
- feat(update): UI dla GitHub tokena + auto-config repo w INSTALL.bat
- feat(installer): embedded Python 3.11 w zipie - klient bez instalacji
- feat(release): HMAC-SHA256 signature zip-a + naming z VERSION
- feat(update): zip-based auto-update dla klientow bez gita (Windows)
- Initial release: akces-hub v1.0.2 for [imie_kolegi]
- fix(db): migrate_reset_fake_data_wystawienia uzywa Polish schema klucz/wartosc
- fix(import): get_config + set_config na top-level w app.py
- fix(privacy+ui): generic launcher / dostawcy placeholder / kiosk LAN configurable
- fix(privacy): WYWAL hardcoded personal data (Adrian Gauza, Poniatowskiego, Mieszkowice...)
- feat(kod_magazynowy): configurowalny prefix per klient (Ty=MAG, Kolega=ZAG)
- feat(magazyn): własny kod_magazynowy + bulk reformat
- feat(magazyn): ręczne dodawanie produktu z formularzem
- fix(ui): kiosk mode OPT-IN (domyslnie wyłączony — klient widzi pełen dashboard)

