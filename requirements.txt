# ============================================================
# Akces Hub - Wymagania produkcyjne
# Instalacja: pip install -r requirements.txt
# ============================================================

# --- Serwer web ---
flask>=3.0.0
flask-cors>=4.0.0
waitress>=3.0.0             # WSGI server produkcyjny (zamiast flask dev server)

# --- HTTP / API ---
requests>=2.31.0
requests-oauthlib>=1.3.1    # Allegro OAuth

# --- Dane / Excel / CSV ---
openpyxl>=3.1.2
pandas>=2.1.0

# --- Obrazki ---
Pillow>=10.0.0
# rembg[cpu]>=2.0.0           # Opcjonalne: lokalne usuwanie tla (500MB+)
# Na Pi: pip install rembg[cpu]

# --- Kody QR i kreskowe ---
qrcode>=7.4.0
python-barcode>=0.15.0

# --- PDF / Etykiety ---
reportlab>=4.0.0

# --- Drukowanie (Bluetooth/USB) --- opcjonalne, tylko na Pi
# python-escpos>=3.1
# bleak>=0.21.0               # BLE (Niimbot)
# niimprint>=0.2.0            # Niimbot SDK
# Na Pi zainstaluj osobno: pip install python-escpos bleak niimprint

# --- Scheduler ---
schedule>=1.2.0

# --- Scraping ---
beautifulsoup4>=4.12.0
lxml>=4.9.0

# --- AI (Gemini) ---
google-generativeai>=0.8.0

# --- System info ---
psutil>=5.9.0

# --- Bezpieczeństwo (NOWE) ---
python-dotenv>=1.0.0        # Ładowanie .env
flask-wtf>=1.2.0            # CSRF protection
flask-limiter>=3.5.0        # Rate limiting
argon2-cffi>=23.1.0         # Argon2id password hashing
defusedxml>=0.7.1           # Bezpieczne parsowanie XML/SVG (XXE, billion laughs)
pyotp>=2.9.0                # TOTP 2FA (Google Authenticator / Authy)
qrcode[pil]>=7.4.2          # QR codes dla setup 2FA
bcrypt>=4.0.0               # Hashowanie backup codes

# --- Testowanie ---
pytest>=8.0.0
