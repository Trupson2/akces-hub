# ============================================================
# Akces Hub - Wymagania produkcyjne
# Instalacja: pip install -r requirements.txt
# ============================================================

# --- Serwer web ---
flask>=3.0.0
flask-cors>=4.0.0
waitress>=3.0.0             # WSGI server produkcyjny (zamiast flask dev server)

# --- HTTP / API ---
requests>=2.31.0
requests-oauthlib>=1.3.1    # Allegro OAuth

# --- Dane / Excel / CSV ---
openpyxl>=3.1.2
pandas>=2.1.0

# --- Obrazki ---
Pillow>=10.0.0
rembg[cpu]>=2.0.0           # Lokalne usuwanie tla (darmowe, bez GPU)

# --- Kody QR i kreskowe ---
qrcode>=7.4.0
python-barcode>=0.15.0

# --- PDF / Etykiety ---
reportlab>=4.0.0

# --- Drukowanie (Bluetooth/USB) ---
python-escpos>=3.1
bleak>=0.21.0               # BLE (Niimbot)
niimprint>=0.2.0            # Niimbot SDK

# --- Scheduler ---
schedule>=1.2.0

# --- Scraping ---
beautifulsoup4>=4.12.0
lxml>=4.9.0

# --- AI (Gemini) ---
google-generativeai>=0.8.0

# --- System info ---
psutil>=5.9.0

# --- Bezpieczeństwo (NOWE) ---
python-dotenv>=1.0.0        # Ładowanie .env
